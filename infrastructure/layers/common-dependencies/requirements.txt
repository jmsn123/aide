# Common dependencies layer - lightweight version for slow networks
# Core essentials only - compatible with aws-lambda-powertools 3.2.0
# Note: AWS Lambda runtime already includes boto3/botocore, so we exclude them
# to avoid version conflicts

# PDF processing - using pypdf for consistency
pypdf==5.1.0
# PDF plumber for advanced table extraction (required by Axis Bank extractor)
pdfplumber==0.11.4
# Explicit pdfminer.six version required by pdfplumber 0.11.4
pdfminer.six==20231228
# charset_normalizer required by pdfminer.six for encoding detection
charset_normalizer

# Cryptography for encrypted PDF support (required by pypdf for AES encryption)
# CRITICAL: These dependencies are MANDATORY and MUST be built for Lambda platform
# CFFI is required by cryptography backend - build process will FAIL if missing
# NO FALLBACKS: If platform-specific build fails, entire layer build fails
cffi>=1.15.0            # Required backend for cryptography in Lambda
cryptography>=3.1       # AES encryption support for password-protected PDFs

# File type detection
python-magic==0.4.27

# Excel processing - needed for Excel export functionality
openpyxl==3.1.2
et_xmlfile==1.1.0

# Date parsing utilities
python-dateutil==2.9.0